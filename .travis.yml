language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-frontify' ]; then cd .. && eval "mv $currentfolder generator-frontify" && cd generator-frontify; fi

